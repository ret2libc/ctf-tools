#!/bin/bash -e

[ -e ropper ] || git clone --depth 1 https://github.com/sashs/Ropper.git ropper

pip install -vvv --no-use-wheel --no-cache-dir -I capstone
pip install -vvv filebytes
pip install -vvv -e ropper
